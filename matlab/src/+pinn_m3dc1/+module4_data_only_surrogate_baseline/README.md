# Module 4: Data-Only Surrogate Baseline

Status: scaffold.

Purpose: Train the fair parameterized neural baseline without physics losses.

This directory currently defines only the MATLAB module interface. It is not an
implemented physical solver or trained surrogate.

