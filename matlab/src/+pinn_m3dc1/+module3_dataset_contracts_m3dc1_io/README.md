# Module 3: Dataset Contracts and M3D-C1-Shaped I/O

Status: physics notes complete; implementation scaffold.

Purpose: Version field arrays, coordinates, parameters, normalization, provenance, and whole-case data splits.

This directory currently defines only the MATLAB module interface. The physics
notes freeze the canonical case schema and Python--MATLAB interoperability
contract, but the readers, writers, validators, manifests, and tests are not yet
implemented.
