# Module 3: Dataset Contracts and M3D-C1-Shaped I/O

Status: physics notes complete; implementation scaffold.

Purpose: Version field arrays, coordinates, parameters, normalization, provenance, and whole-case data splits.

This directory currently defines only the module interface. The physics notes
freeze the canonical case schema, provenance, physical-case grouping, splits,
preprocessing, versioning, and M3D-C1 adapter boundary. The writers, validators,
manifests, and interoperability tests are not yet implemented.
