# Module 5: Physics-Informed Surrogate

Status: scaffold.

Purpose: Train single-case and parameterized PINNs with reduced-MHD residual and constraint losses.

This directory currently defines only the module interface. It must not be
reported as an implemented physical solver or trained surrogate until its
implementation note, verification tests, and acceptance criteria are complete.

