# Module 5: Physics-Informed Surrogate

Status: physics notes complete; implementation scaffold.

Purpose: Train single-case and parameterized PINNs with reduced-MHD residual and constraint losses.

The governing residuals, constraints, training curriculum, verification tests,
and acceptance criteria are documented in
`docs/physics_notes/module5_physics_informed_surrogate.pdf`. This directory
still defines only the module interface. It must not be reported as an
implemented or trained surrogate until the implementation and tests exist.
