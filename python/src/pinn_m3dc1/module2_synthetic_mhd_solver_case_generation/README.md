# Module 2: Synthetic MHD Solver and Case Generation

Status: physics notes complete; implementation scaffold.

Purpose: Generate and verify periodic island-coalescence trajectories with an independent conventional solver.

This directory currently defines only the module interface. The physics notes
freeze the numerical method, verification tests, and acceptance criteria, but
the module must not be reported as an implemented physical solver until the
solver code passes those checks.
