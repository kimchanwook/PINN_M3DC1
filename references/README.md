# References and provenance

This directory records the exact external sources used for equations,
benchmark definitions, M3D-C1 variable mappings, and validation comparisons.
Do not store screenshots as numerical training data. For every imported result,
record citation, code/version, variable definitions, normalization, mesh,
boundary conditions, and any conversion performed by the project.

