Implementation notes will mirror the seven source modules. Each note should
freeze inputs, outputs, array shapes, units or normalization, algorithms,
failure behavior, tests, and acceptance criteria before the corresponding
module is considered implemented.

